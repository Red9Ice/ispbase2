# Imlight Show Production

Система управления мероприятиями, персоналом, оборудованием и всеми связанными процессами для организации шоу-продакшена.

## 📋 Описание

**Imlight Show Production** — это комплексная система для управления жизненным циклом мероприятий, включающая:

- 📅 **Управление мероприятиями** — планирование, отслеживание статусов, бюджетирование
- 👥 **Управление персоналом** — профили сотрудников, доступность, назначения на мероприятия
- 📦 **Склад и оборудование** — инвентаризация, категории, наборы, кейсы, отслеживание перемещений
- 🏢 **Клиенты и площадки** — база клиентов и площадок для мероприятий
- 📊 **Дашборд и отчеты** — визуализация данных, аналитика, отчеты по утилизации персонала
- ✅ **Задачи и назначения** — управление задачами и назначениями персонала на мероприятия
- 📝 **История изменений** — полный аудит всех изменений в системе

## 🚀 Технологии

### Backend
- **Node.js** + **Express** — серверная часть
- **TypeScript** — типизированный JavaScript
- **PostgreSQL** — реляционная база данных
- **JWT** — аутентификация и авторизация
- **bcryptjs** — хеширование паролей

### Frontend
- **React 19** — пользовательский интерфейс
- **TypeScript** — типизация
- **Vite** — сборщик и dev-сервер
- **React Router** — маршрутизация
- **Recharts** — графики и визуализация данных
- **React Grid Layout** — адаптивные виджеты дашборда
- **Three.js** — 3D визуализация (опционально)

## 📦 Установка

### Требования
- Node.js 18+ 
- PostgreSQL 12+
- npm или yarn

### Backend

```bash
cd backend

# Установка зависимостей
npm install

# Создание файла .env на основе .env.example
cp .env.example .env

# Настройка переменных окружения в .env
# DATABASE_URL=postgres://user:password@localhost:5432/imlight
# JWT_SECRET=your_secret_key
# JWT_REFRESH_SECRET=your_refresh_secret

# Запуск в режиме разработки
npm run dev

# Или сборка и запуск production
npm run build
npm start
```

Backend будет доступен на `http://localhost:3001`

### Frontend

```bash
cd frontend

# Установка зависимостей
npm install

# Создание файла .env (опционально)
# VITE_API_URL=http://localhost:3001/api/v1

# Запуск в режиме разработки
npm run dev

# Или сборка для production
npm run build
npm run preview
```

Frontend будет доступен на `http://localhost:5173`

## 🗄️ База данных

### Создание базы данных

```bash
# Подключение к PostgreSQL
psql -U postgres

# Создание базы данных
CREATE DATABASE imlight;

# Создание пользователя (опционально)
CREATE USER your_user WITH PASSWORD 'your_password';
GRANT ALL PRIVILEGES ON DATABASE imlight TO your_user;
```

### Миграции

Миграции базы данных находятся в `backend/src/migrations/`. При первом запуске приложения миграции применяются автоматически.

## 🔐 Аутентификация

Система использует JWT токены для аутентификации:
- **Access Token** — короткоживущий токен для доступа к API
- **Refresh Token** — долгоживущий токен для обновления access token

Токены хранятся в HTTP-only cookies для безопасности.

## 📁 Структура проекта

```
ispbase2/
├── backend/              # Backend приложение
│   ├── src/
│   │   ├── modules/      # Модули приложения
│   │   │   ├── events/   # Мероприятия
│   │   │   ├── staff/    # Персонал
│   │   │   ├── equipment/# Оборудование
│   │   │   ├── auth/     # Аутентификация
│   │   │   └── ...
│   │   ├── common/       # Общие утилиты
│   │   └── main.ts       # Точка входа
│   ├── dist/             # Скомпилированный код
│   └── package.json
├── frontend/             # Frontend приложение
│   ├── src/
│   │   ├── components/   # React компоненты
│   │   ├── services/     # API сервисы
│   │   ├── contexts/     # React контексты
│   │   └── ...
│   └── package.json
└── docs/                 # Документация
    ├── project.md        # Описание проекта
    ├── tasktracker.md    # Отслеживание задач
    └── ...
```

## 🎯 Основные возможности

### Мероприятия
- Создание и редактирование мероприятий
- Фильтрация по статусу, датам, клиенту, площадке
- Отслеживание бюджета
- Назначение персонала и оборудования

### Персонал
- Управление профилями сотрудников
- Отслеживание доступности
- Назначения на мероприятия
- Отчеты по утилизации

### Склад
- Управление оборудованием
- Категории и подкатегории
- Наборы оборудования
- Кейсы для транспортировки
- Отслеживание перемещений

### Дашборд
- Адаптивные виджеты
- Графики и аналитика
- Виджет погоды
- Кастомная компоновка

## 🔧 Разработка

### Запуск в режиме разработки

```bash
# Terminal 1 - Backend
cd backend
npm run dev

# Terminal 2 - Frontend
cd frontend
npm run dev
```

### Линтинг

```bash
# Backend
cd backend
npm run lint

# Frontend
cd frontend
npm run lint
```

## 📝 API

API документация доступна в `docs/api.md`

Основные эндпоинты:
- `/api/v1/auth/*` — аутентификация
- `/api/v1/events/*` — мероприятия
- `/api/v1/staff/*` — персонал
- `/api/v1/equipment/*` — оборудование
- `/api/v1/history/*` — история изменений

## 🛡️ Безопасность

- Ролевая модель доступа (RBAC)
- Хеширование паролей (bcrypt)
- JWT токены в HTTP-only cookies
- Валидация всех входных данных
- Аудит всех критических действий

## 📊 История изменений

Система ведет полную историю изменений всех сущностей:
- Кто внес изменение (actor_id)
- Когда (created_at)
- Что изменилось (action, entity_type, entity_id)
- Старые и новые значения (old_values, new_values)

История хранится 1 год, затем автоматически удаляется.

## 🤝 Вклад в проект

1. Fork проекта
2. Создайте ветку для фичи (`git checkout -b feature/AmazingFeature`)
3. Закоммитьте изменения (`git commit -m 'Add some AmazingFeature'`)
4. Push в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📄 Лицензия

Этот проект является приватным и предназначен для внутреннего использования.

## 👤 Автор

**Red9Ice**

## 📞 Контакты

Проект: [https://github.com/Red9Ice/ispbase2](https://github.com/Red9Ice/ispbase2)

---

**Версия:** v0.1 MVP ALPHA

**Статус:** В активной разработке
